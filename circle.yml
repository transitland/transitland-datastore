machine:
  ruby:
    version: 2.2.3

test:
  override:
    - RAILS_ENV=test bundle exec rspec -r rspec_junit_formatter --format RspecJunitFormatter -o $CIRCLE_TEST_REPORTS/rspec/junit.xml
    - mkdir -p $CIRCLE_ARTIFACTS/profiling
    - RAILS_ENV=test bundle exec rake profile:import[$CIRCLE_ARTIFACTS]

deployment:
  staging:
    branch: master
    commands:
      - ./deploy/env.rb staging
  production:
    branch: production
    commands:
      - ./deploy/env.rb prod
