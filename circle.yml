machine:
  ruby:
    version: 2.1.2

test:
  override:
    - bundle exec rake

deployment:
  staging:
    branch: master
    commands:
      - ./deploy/env.rb staging
  production:
    branch: production
    commands:
      - ./deploy/env.rb prod
