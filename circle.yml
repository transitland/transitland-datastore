version: 2
jobs:
  rails-test-suite:
    docker:
      - image: ruby:2.3.1
        environment:
          RAILS_ENV: test
          DATABASE_URL: postgis://root@localhost/transitland_datastore_test
      - image: mdillon/postgis:9.5
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: transitland_datastore_test
    steps:
      - checkout
      - restore_cache:
          keys:
            - transitland-datastore-{{ checksum "Gemfile.lock" }}
            - transitland-datastore-
      - run:
          name: Install dependencies
          command: apt-get update -qq && apt-get install -y libgeos-dev libproj-dev
      - run:
          name: Install bundle
          command: bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs 4 --retry 3
      - save_cache:
          key: transitland-datastore-{{ checksum "Gemfile.lock" }}
          paths:
            - vendor/bundle
      - run:
          name: Database setup
          command: bundle exec rake db:drop db:create db:schema:load
      - run:
          name: Run tests
          command: bundle exec rake
      # - run:
      #     name: Run profile
      #     command: mkdir -p $CIRCLE_ARTIFACTS/profiling && bundle exec rake profile:import:nycdot[$CIRCLE_ARTIFACTS/profiling]
  build-container-image:
    docker:
      - image: circleci/buildpack-deps:16.04-curl
    steps:
      - checkout
      - run: apt-get update -qq && apt-get install python-pip python-dev -y
      - run: pip install awscli --upgrade --user
      - run: export PATH=~/.local/bin:${PATH}
      - run: export DOCKER_TAG=${CIRCLE_TAG:-$CIRCLE_BRANCH}
      - setup_remote_docker
      - run: eval $(aws ecr get-login --no-include-email)
      - run: docker build -t transitland-datastore:${DOCKER_TAG} .
      - run: docker tag transitland-datastore:${DOCKER_TAG} ${TRANSITLAND_AWS_ECR}/transitland-datastore:${DOCKER_TAG}
      - run: docker push ${TRANSITLAND_AWS_ECR}/transitland-datastore:${DOCKER_TAG}
workflows:
  version: 2
  test-and-build-workflow:
    jobs:
      - build-container-image
      - rails-test-suite

