<!DOCTYPE html>
<html>
<head>
  <title>Transitland Datastore Admin</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <style>
    html, body, .container {
      height: 100%;
    }
    body {
      padding-top: 80px;
    }
    iframe {
      height: 95%;
      width: 100%;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'Transitland Datastore Admin', root_path, class: 'navbar-brand' %>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if current_page? root_path %>"><%= link_to 'Main', root_path %></li>
          <li class="<%= 'active' if current_page? dispatcher_path %>"><%= link_to 'Dispatcher', dispatcher_path %></li>
          <li class="<%= 'active' if current_page? sidekiq_dashboard_path %>"><%= link_to 'Sidekiq Dashboard', sidekiq_dashboard_path %></li>
          <li class="<%= 'active' if current_page? postgres_dashboard_path %>"><%= link_to 'Postgres Dashboard', postgres_dashboard_path %></li>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container">
    <% if flash[:success] %>
      <div class="alert alert-success" data-alert="alert">
        <%= flash[:success] %>
      </div>
    <% end %>
    <% if flash[:info] %>
      <div class="alert alert-info" data-alert="alert">
        <%= flash[:info] %>
      </div>
    <% end %>
    <% if flash[:error] %>
      <div class="alert alert-error" data-alert="alert">
        <%= flash[:error] %>
      </div>
    <% end %>
    <% if flash[:warning] %>
      <div class="alert alert-warning" data-alert="alert">
        <%= flash[:warning] %>
      </div>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
